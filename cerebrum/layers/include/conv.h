#ifndef CONV_H_
#define CONV_H_

namespace layer {
  class Conv {
    public:
      Conv(

    private:
      size_t size;
      size_t width;
      size_t depth;
      size_t filter_size;
      size_t filter_width;
      size_t horizontal_stride;
      size_t vertical_stride;
  };
} // namespace layer

#endif // CONV_H_
