#include <cassert>
#include "./include/runtime.h"

namespace engine {
  void Runtime::fit(const arma::vec& inputs, const std::string optimizer) {
    assert(layers.size() != 0);
  }
} // namespace engine
